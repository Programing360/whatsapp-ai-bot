[phases.setup]
# Install native standalone Chromium from Nixpkgs (avoids Ubuntu apt snap wrapper)
nixPkgs = ["chromium"]

[variables]
# Use Nix Chromium
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/root/.nix-profile/bin/chromium"
NODE_ENV = "production"
